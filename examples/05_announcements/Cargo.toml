[package]
name = "announcements"
version = "0.1.0"
edition = "2021"
description = "Broadcasts server messages at regular intervals using timers"

[lib]
crate-type = ["rlib"]

[dependencies]
cs2rust-core.workspace = true
tracing.workspace = true
