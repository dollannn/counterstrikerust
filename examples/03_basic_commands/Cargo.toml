[package]
name = "basic_commands"
version = "0.1.0"
edition = "2021"
description = "Demonstrates console and chat command registration"

[lib]
crate-type = ["rlib"]

[dependencies]
cs2rust-core.workspace = true
tracing.workspace = true
