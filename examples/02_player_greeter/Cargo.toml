[package]
name = "player_greeter"
version = "0.1.0"
edition = "2021"
description = "Welcomes players when they join and announces departures"

[lib]
crate-type = ["rlib"]

[dependencies]
cs2rust-core.workspace = true
tracing.workspace = true
